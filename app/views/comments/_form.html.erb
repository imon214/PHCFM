<div id="articles">
<%= form_for [@parent, @comment] do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :name %>: <%= f.text_field :name %>
    </div>
    <div class="field">
<%= f.label :email %>: &nbsp;<%= f.email_field :email, :placeholder => "Please put a valid email" %>
    </div>
    <div class="field">
      <%= f.label :body %><br />
      <%= f.text_area :body, {:rows => 8, :cols => 80, :placeholder => 'Your Message Here..'} %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>