<%= form_for(@about) do |f| %>
  <% if @about.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
      <% @about.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, :size => 100 %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, {:rows => 20, :cols => 110} %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link, :size => 100 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
